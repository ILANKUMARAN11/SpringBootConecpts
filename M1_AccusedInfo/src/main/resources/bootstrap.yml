server:
    port: 8010

spring:
    application:
        name: Accused
    jmx:
        default-domain: Accused
    profiles:
            active: default
    cloud:
        config:
            discovery:
                enabled: true
                serviceId: configurationServer
            username: ilan
            password: ilan
