server:
    port: 8020

spring:
    application:
        name: Crime
    jmx:
        default-domain: Crime
    profiles:
            active: default
    cloud:
        config:
            uri: http://localhost:8888
            username: ilan
            password: ilan
